<Window x:Class="DreamWin.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DreamWin"
        Width="360" Height="220"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Topmost="True">
    <Border Background="{StaticResource BgDeep}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="14">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo + wordmark, vertically centered -->
            <StackPanel Grid.Row="0" Orientation="Horizontal"
                        HorizontalAlignment="Center" VerticalAlignment="Center">
                <Ellipse x:Name="LogoDot" Width="34" Height="34" Fill="{StaticResource Accent}" Margin="0,0,12,0">
                    <Ellipse.Effect>
                        <DropShadowEffect Color="#6C63FF" BlurRadius="20" ShadowDepth="0" Opacity="0.9"/>
                    </Ellipse.Effect>
                    <Ellipse.RenderTransform>
                        <ScaleTransform x:Name="LogoScale" ScaleX="1" ScaleY="1" CenterX="17" CenterY="17"/>
                    </Ellipse.RenderTransform>
                </Ellipse>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="DREAM" FontSize="26" FontWeight="Black" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="WIN" FontSize="26" FontWeight="Black" Foreground="{StaticResource Accent}"/>
                </StackPanel>
            </StackPanel>

            <!-- Status text -->
            <TextBlock Grid.Row="1" x:Name="StatusText" Text="Loading…"
                       FontSize="11" Foreground="{StaticResource TextSecondary}"
                       HorizontalAlignment="Center" Margin="0,0,0,14"/>

            <!-- Indeterminate progress bar -->
            <Grid Grid.Row="2" Margin="40,0,40,28" Height="3" VerticalAlignment="Center">
                <Border Background="{StaticResource BgCard}" CornerRadius="2"/>
                <Border x:Name="ProgressFill" Background="{StaticResource Accent}" CornerRadius="2"
                        HorizontalAlignment="Left" Width="70">
                    <Border.RenderTransform>
                        <TranslateTransform x:Name="ProgressTranslate" X="-70"/>
                    </Border.RenderTransform>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
